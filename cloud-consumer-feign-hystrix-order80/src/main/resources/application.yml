server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
#开启hystrix让80客户端支持服务降级
feign:
  hystrix:
    enabled: true

